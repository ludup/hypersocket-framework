package com.hypersocket.realm;

import java.util.Map;

public interface PrincipalProcessor {

	void beforeUpdate(Principal principal, Map<String,String> properties);
	
	void afterUpdate(Principal principal, Map<String,String> properties);

	void afterCreate(Principal principal, Map<String, String> properties);
}
